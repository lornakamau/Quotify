import { DateSubmittedPipe } from './date-submitted.pipe';

describe('DateSubmittedPipe', () => {
  it('create an instance', () => {
    const pipe = new DateSubmittedPipe();
    expect(pipe).toBeTruthy();
  });
});
